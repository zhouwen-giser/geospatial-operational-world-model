# Elevation Mock Provider

`elevation.sample.mock@1.0` is the conformance canary for future capabilities.
It is intentionally non-production and deterministic. Registering it requires
only a controlled manifest/binding; the Gateway contains no operation-specific
code.
