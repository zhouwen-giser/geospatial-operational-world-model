# 示例，必须由部署者填写。不要提交真实凭据。
# 使用现有GOWM数据库名，不创建每设备数据库。
GOWM_DATABASE_URL=
GOWM_BUSINESS_TEST_DATABASE_URL=
GOWM_BUSINESS_SMOKE_ENABLE=false
# 固定Schema: Runtime和UGV Provider -> ugv_smpp; SDAR -> ugv_sdar
# device_id由每次业务写入明确传递，不设置DEFAULT_DEVICE_ID=ugv1。
# 消费者连接和迁移模式适配在各自后续任务完成。
