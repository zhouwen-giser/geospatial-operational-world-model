# UGV 设备主档 actor 验证记录

日期：2026-09-08。实现范围仅 GOWM；未切换实际 MQTT、SMPP 或 SDAR 服务。

## 已完成

- 显式、幂等的默认设备初始化和只读校验 CLI；复用采集来源、主档和七条数据流，冲突回滚。
- 主档 ID、外部设备标识、WORLD_OBJECT 引用分别保存；新版 mapper v3 事件 actor 指向主档。
- 设备范围内的业务默认解析与持久服务绑定；支持后续 SDAR 补充缺失字段，保留旧绑定及微秒精度有效期边界。
- 冻结持久会话中的设备与 actor 引用版本；兼容 v1/v2 旧消息，不回填历史事件。
- actor 查询按引用身份匹配，保留事件中的原始版本。
- 迁移 079 提供只读主档访问角色；安装器、部署打包及分仓交接材料包含该迁移。

## 验证结果

| 验证 | 结果 |
| --- | --- |
| 全量 Vitest | 673 passed；有环境前提的集成套件默认跳过，新增 actor 套件另行实测 |
| 新增 actor PostgreSQL 集成 | 12 passed：重复初始化、CLI、默认服务绑定、双设备、冲突、禁用/退役/跨范围、只读角色、观测落库、事件查询、重连/重投、历史不变 |
| 既有共享业务 PostgreSQL 回归 | 42 / 42 passed；安装器 install / verify 通过 |
| TypeScript 全仓类型检查 | PASS |
| 冻结迁移 001–068 检查 | PASS；未修改冻结迁移 |
| 109 原生迁移及核心清单校验 | PASS |
| 实际部署镜像构建 | PASS |
| 镜像源码与运行实现一致性 | PASS，790 个文件 |
| 非 root 迁移读取及两个 CLI 帮助入口 | PASS |
| 部署包校验、权限、排除规则、可复现性 | PASS |

所有数据库验证均在 GOWM 专用隔离容器的克隆测试数据库执行；未操作运行中的业务数据库。

## 部署产物

- `output/deployment/gowm-dev-server-0.7.1.tar.gz`
- SHA256：`33a72b038d7d4e4f8589c4048534391457b1e47a047061253e95bb59f631cbf6`
- 验证镜像：`gowm-dev-package-check:33a72b038d7d4e4f`
- 原部署包已自动备份。

上线按 `docs/UGV_DEVICE_ACTOR.md` 执行迁移、默认初始化、角色授权、只读检查和新会话切换。
SMPP/SDAR 消费端代码及服务切换由各仓后续任务完成，GOWM 接入接口和说明已交付。
