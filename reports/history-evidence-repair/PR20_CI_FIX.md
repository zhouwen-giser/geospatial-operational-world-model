# PR #20 CI 修复

失败 run：34102508200，candidate-required-check 的 067→069 升级步骤。
根因：旧精确账本测试调用迁移器时未给上限，新增迁移 071–075 被一起执行，
随后“必须精确为 69 条”的断言失败。迁移本身没有在该日志中失败。

修复保留精确断言：旧测试显式传 69，helper 参数改为必填，防止再次省略。
生产 migrate 默认仍执行全部迁移，不人为停在 069。
独立 history-evidence-ci 门禁负责新建库和 069→075，显式固定自己的 075 边界，
检查前置账本及重放 checksum 不变，并运行真实 PG 原始证据、UNKNOWN、
专用调度器、旧修订恢复、晚到数据和权限隔离验收。
PR 和 main 工作流均运行此新增门禁，没有删除、跳过或放宽旧检查。

本地 PostgreSQL 18.6 / PostGIS 3.6.4 / MobilityDB 1.3.0 复验：

- 旧 001→069 / 067→069：PASS。
- 新 001→075 / 069→075：PASS，197 原始采样 / 2 几何节点，PROVISIONAL。
- 旧 Gateway 两 Provider DAG、快照、事件集合推进：PASS。
- 临时测试库创建后自动删除，不修改持久部署数据。

另已整合 PR #19 的 main 历史；冲突保留本分支较新的 UGV 历史包及其
97c275…ddd55 companion，未重建或发布新包。现场重放与发布边界不变。
