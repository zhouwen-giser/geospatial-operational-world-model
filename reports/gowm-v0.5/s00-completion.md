# S00 Documentation and Version Completion

The repository version, root npm package metadata, lockfile root, and changelog now agree on `0.5.0`. The frozen v0.4 contract lock remains `0.4.0`; v0.5 contract schemas and Provider manifests remain byte-locked by generated/runtime checks.

README, ADR 005, the operations runbook, and PROJECT_STATUS describe the implemented authority boundary and actual runtime evidence. They distinguish a pinned computational Route Plan from dispatch, device execution, observed Operational Reality, and physical completion. Regional road coverage, multi-vehicle optimization, production IdP/HA/PITR, and production-sized SLO/capacity qualification are explicit non-claims.

The runbook records migrations 001–047, scoped Network/Route role behavior, exact Avoid Area authority, Condition Snapshot handling, immutable revalidation, restart/idempotency checks, and controlled Docker recovery without deleting persistent state.
