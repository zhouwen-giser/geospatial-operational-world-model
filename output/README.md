# Runtime evidence output

This directory is intentionally empty in the release package. `npm run acceptance`
and benchmark commands write machine-specific evidence here. Results inherited
from v1.1 were removed so they cannot be mistaken for v1.2 runtime validation.
